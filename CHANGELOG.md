# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

- 权限与模型两个按钮去掉右侧的下箭头（输入区工具栏进一步精简）。
- 子代理列表不再需要点开面板才出现：子代理一建立就注册进列表（从会话日志里的建立事实读，直播与重载重放都会走一遍），打开会话时也会向服务端拉一次完整目录，重载窗口/切回会话后直接就有。
- 子代理的运行状态改为实时：服务端状态位一到就更新那一行的状态点，头部子代理入口的呼吸不再要等点开面板。
- 修复：子代理目录请求失败时会把面板清空（一次瞬时故障显示成「当前会话没有子代理」），现在保留已有列表。
- 会话开始后，输入框工具栏在思考强度与附件按钮之间显示当前会话的 agent 预设名（只读标签，不可点、不可改）：空态页那个可选的下拉框在第一条消息之后就不再出现，这里补上「这套组装叫什么」。
- 修复：新会话页工作目录与 agent 预设两行的图标偏上、与文字不对称，现按文字字形的视觉重心对齐。
- 没有打开文件夹时，新会话页不再拿 VS Code 的安装路径当工作目录：那一行显示「未选择工作区」，点它可以挑一个目录；发第一条消息时若还没挑，会先弹一次目录选择器。
- 新会话记录改为**发第一条消息时才建**：此前点一次「新建对话」（或选一次目录）就会在列表里留下一条没人用过的空会话，现在只把窗口退回空态，选目录/预设/模型都只是待建会话的参数。
- 新会话页显示这次会话的工作目录：VS Code 打开了文件夹时跟随它（不可改），没打开时可以点那一行换一个目录。
- 新会话页新增 agent 预设下拉框（官方菜单那种卡片：名字 + 最多三行描述 + 选中对勾）；新会话默认用哪个预设由新增的 `dshChat.agentPreset` 决定，留空则跟随 DSH 服务端的默认预设。
- 修复：子代理面板里点开任一子代理都显示「这个子代理没有可显示的内容」，现在能看到它的会话记录（只读、默认停在最新一条）。
- 修复：生成中离开会话再回来（切到别的会话、重载窗口、掉线重连），会话可能一直显示成空闲——底部没有「深度求索中」，按钮是「发送」，此时发出去的消息会被排进队列。现在「是否在生成」由服务端状态位与会话列表共同维护，不再只从会话日志推断。

## 0.9.0（2026-09-21）

- **生成中也能加载更早的历史**：此前生成期间按钮是灰的（提示「请等这一轮结束」），现在与官方 Web 端一致，只在真正取历史的那一刻禁用。
- 空输入框的占位文案改为「发消息或创建任务，/ 调用指令，@ 文件或对话」，三条入口都点到。
- 修复：重载 VS Code 后，编辑区标签与恢复出来的会话对不上；标签现在显示会话标题。
- 修复：生成中离开会话再回来，思考节点分裂成两条（前一条只剩一个字、且一直显示在跑）。
- 修复：思考 / 正文节点从「生成中」转为「已完成」时被重挂，你手动展开的节点会自己收回去。
- 工具结果里的图片改为**展开才渲染**，收起时不再占位（此前折叠态也画图）。
- 修复：拖放或粘贴进来的文件附件上传成功却不进 prompt。
- 新增：会话页任意位置 `Ctrl+V`，剪贴板里的图片（截图）与文件直接进附件栏；目录按 `@目录/` 引用插入正文。
- 展开的节点：**还在跑的贴底**（看得到最新输出）、已结束的置顶；上滑即脱跟随，滑回底部恢复。
- 「加载更早的历史」改为按需两档：按钮点一次取一页；点轮次横条上未加载的刻点会先取回那一轮再跳过去。取消滚动到顶自动加载。
- 轮次结束后，助手消息尾部显示**本轮改动文件卡片**：文件数、`+a −d` 合计与逐文件明细（折叠 3 行，可展开）。
- 待答问卷卡新增「放弃整组问题」。
- 子代理面板：跑完的子代理读作「已完成」（绿灯）而不是「未运行」；点进去可看它的会话记录（只读，默认停在最新一条）；分支会话与普通会话同级显示；有子代理或后台任务在跑时，右上角对应的入口会亮起并呼吸。
- 「本客户端不认识的事件」会写进「DSH Chat」输出通道（每种只记首见一条），一闪而过的提示条从此可追溯。
- 配置项 `dshChat.autoConnect` 改动即时生效，不再提示重载窗口。

## 0.8.2（2026-09-19）

连接机制定型为「内部 DSH 优先、外部 DSH 备用」，并修掉实测中三处「点了没反应」。

- 启动后自动选一次连接目标并有粘性：内部守护进程在跑就接内部，否则 `dshChat.url` 可达就接外部，都没有就拉起一套内部；重试只认这个目标，中途不会换服务器。
- 配置项 `dshChat.autoStart` 改名为 `dshChat.autoConnect`（关掉 = 启动后完全不自动连，只显示按钮），旧键不兼容。
- 连接条按钮改为「启动内部 DSH」/「连接内部 DSH」/「连接外部 DSH」/「重启内部 DSH」；连接中只留「停止连接」+「查看日志」；命令面板同步新增「DSH: 连接内部 DSH」「DSH: 连接外部 DSH」。
- 连接类失败（地址连不上、socket 断开）一直重试、不自动停止；启动类与认证类失败不重试，退回按钮态并显示原因。
- 修复：切到外部后内部 DSH 仍被反复拉起，诊断里显示的也还是内部。
- 修复：点「停止连接」停不下来；停止后不再占用内部后台（不连就不占用），此后只由显式动作重新连接。
- 修复：「停止连接」之后点「重启内部 DSH」会假报「已重启」，现在真会重启。

## 0.8.1（2026-09-18）

- 新增右侧轮次横条：点刻度跳到该轮（窗口外的轮次先取回历史再落位），悬停给出预览；会话区窄于 360px 时自动撤掉。
- 修复：只发一条消息时，横条上不再冒出「第 0 轮」。

## 0.8.0（2026-09-18）

- 会话窗口里恢复显示待发消息列表（此前消息确实进了队列、也能正常发出，界面却不显示「待发送 N 条」）。
- 图片在会话里直接可见：你发出的图片显示缩略图，agent 交付 / 生成的图片画成图，正文引用的本地与外链图片按原样渲染，工具结果里的图片画在工具行下方；点击任意图片看原图，`Esc` 关闭且不会顺带中止正在生成的一轮。
- 本地图片解析失败不再静默：界面提示「图片加载失败」并带上原始路径，输出通道里也能看到是哪几张没解析出来。
- 修复：多行草稿每敲一个字，会话页底部就抖一下。
- 修复：生成中会突然不再贴底且一直不恢复。现在只有你主动上滑（滚轮 / 触摸 / 键盘 / 拖滚动条）才停止跟随，脱贴时出现「回到最新」胶囊。
- 新增配置项 `dshChat.turnProcessThreshold`：过程折叠阈值，默认 5；`0` = 永不折叠，`1–2` = 永远折叠；改完即时生效。
- 轮级过程折叠改为只折起「最后那段回答」之前的内容，中途正文与工具调用折进按钮（按钮显示工具调用次数、消息条数与 subagent 数），最后那段回答始终留在流里。
- 工具行改为按语义显示卡片（读取、搜索、终端、网页搜索、代码执行、待办清单…），不再摊开完整参数 JSON；展开体分输入 / 输出两段，折叠行右侧显示 `+N -M`，路径可直接点击。
- 复制反馈调整：不再弹「已复制」提示条，改由按钮自己短暂换文案；网页搜索卡补上复制按钮；整轮只有工具调用、没有正文时轮尾不出现复制按钮；生成过程中轮尾操作行整体不显示。
- 修复：切会话或重载窗口后问卷丢失、agent 一直卡在问卷步骤；问卷卡限高可内部滚动，出现问卷时轨迹页与输入区不再被压扁；问卷 / 审批卡在多窗口下只出一张，别人答完或这一轮被中止后本窗口的卡会自动收场。
- 问卷的「自定义回答」是多行输入框：点它即选中、再点一下取消，`Enter` 换行、`Ctrl/Cmd+Enter` 提交；改选别的选项不清空已写内容；答完的问卷展开后能看到当时选了什么。
- 计划模式下模型提交计划时显示审阅卡，包含完整计划正文；卡上按钮用界面语言显示。
- 轨迹页打开时占满整个会话窗口，切回会话时问卷与已填写的选择原样保留；轨迹支持滚轮缩放、右键平移、双击复位，点选或框选后下方账本跳到对应记录。
- 展开工具行、思考行等节点时不再把内容向上挤：默认停在内容顶部，展开时暂停自动贴底，滚回底部恢复跟随。
- 写入类工具节点固定单栏显示；命令行节点不再出现两层垂直滚动条。
- 把文件拖进会话页面任意位置都能作为附件上传（此前只有输入框能接收）；拖拽期间整页提示「松开即添加」；纯文本拖拽不受影响。VS Code 主窗口仍可能拦截拖放，按住 `Shift` 可放行。
- 顶部齿轮改为「在浏览器中打开 DSH Web」（地球图标），用系统默认浏览器打开官方 Web 首页，设置在那里改。
- 历史会话列表跟随当前工作区，没有打开文件夹时只显示「未分组」；修掉「切到 B 会话，重启后又回到 A」。
- `@` 引用补上「对话」组（可引用其它会话）；`Enter` 载入候选、`Tab` 进入目录；编辑器选区与资源管理器右键添加都改为插入 `@文件#L12-L40` / `@目录/` 形式的引用。
- 历史加载改为一次取回全部，按钮文案「加载全部历史」。
- 用户消息超过 5 行默认收缩，「展开 / 收起」放在轮尾操作行最右侧；每轮的复制 / 分支 / 用时不再需要悬停才出现。
- 后台任务面板不再把正在停止 / 已取消的任务显示成运行中，在跑的任务恒排最前。
- dsh 崩溃后会被自动重新拉起；守护进程的内部错误会写进「DSH Chat」输出通道，不再静默消失。
- 「停止连接」在正在连接时也能点，点完不会被心跳自动接回；「尝试重连」改名「尝试连接」；「查看日志」在连接条上恒显，且真的会打开输出通道。
- 删除配置项 `dshChat.startTimeoutSec`：连接不再按时长判定失败，不再每 90 秒冒一次假的「启动超时」。
- 删除配置项 `dshChat.openPanelOnStartup`；编辑框下方的速度改为平均输出速度。
- 编辑器标题栏新增「在本分组新建对话窗口」。
- 修复一批问题：切会话后「加载更早」永久卡死、工具结果行永远停在「运行中」、提示条不消失、按 `Esc` 会连带中止正在生成的一轮、子代理面板跨会话不刷新、轨迹页「输出」行显示的是时长、Diff 页签固定英文、一轮结束后抢焦点。
- `dshChat.command` 与 `dshChat.url` 不再允许被工作区设置覆盖（写在某个工作区设置里的值会被忽略，需改到用户设置）。
- 扩展的运行时数据目录迁到 `~/.dsh/dsh-chat-vscode`，旧目录 `~/.dsh-chat` 不再被读取。
- 界面语言不再等到连接完成后才生效：启动即按 VS Code 显示语言（或 `dshChat.language`）显示，不再先英文后中文。
- 已知限制：子代理转录面板里的图片仍不显示。

## 0.7.0（2026-09-14）

- 后台 dsh 改由独立守护进程托管：重载窗口、打开文件夹、装扩展都不再打断后台，关闭 VS Code 也不再留下连不上的残留后台。
- 全部窗口关闭或崩溃后，后台在 `dshChat.supervisorIdleSec`（新增配置项，默认 10 秒，可设 5~600）内保留，超时才连同 dsh 一起退出。
- 多窗口共用一个后台，直接用后台的令牌接入，不再弹出你手里根本没有的「输入令牌」；崩溃或未正常关闭留下的后台，下次激活会被接管。
- 关闭 `dshChat.autoStart` 后扩展不再自行拉起后台，只显示「启动服务器」按钮；你的显式动作（发消息、新建 / 切换会话、启动 / 重启服务器）不受该配置限制。
- 连接条按状态给按钮：未启动显示「启动服务器」，连接中显示「停止连接」（只停本窗口的重试，不动后台），连不上时给原因与「尝试重连 / 重启服务器 / 查看日志」；连接失败会一直重试。
- 新增命令「DSH: 停止服务器」（后台连同 dsh 一起退场）；「重启服务器」改为请后台重起 dsh。
- 编辑器右键加进来的代码引用带行号（芯片显示 `src/config.ts:12-40`），发给模型的内容里也写明行范围；加完引用后焦点还给最近活动的那个对话窗口。
- 跟随窗口只带最近 60 条消息，滚到距顶 64px 以内会自动加载更早的历史（一次一页，加载期间按钮显示「正在加载更早消息…」）；手动按钮保留。
- 问卷题数超过 `dshChat.questionBatch`（新增配置项，整数 ≥ 0，默认 3，`0` = 始终一次展开）时改成一次一道，带「第 N / M 题」与上一题 / 下一题；答完的问卷默认收缩成一行，可点开复看。
- 重开工作区时，各对话窗口（主侧栏 / 辅助侧栏 / 编辑区面板）各自接回上次的会话；会话已被删除或归档时窗口保持空态；不往项目目录写文件。
- 目标条正文一行放得下时不再显示展开按钮；访问令牌输入框的说明改为「存下来的是 token 换来的会话 cookie」，中文界面下显示中文说明。
- 轮尾「本轮改动」不再显示净效果为零的临时文件（如 `commit.msg.txt`）。
- 新会话会记进工作区注册表，在 DSH Web 上按工作区分组（此前一律「未分组」；已建好的会话不追溯）。
- 新增 markdown 脚注支持：`[^label]` 渲染为上标编号，定义收进文末脚注区。
- `/` 与 `@` 候选列表随键盘上下键滚动到可见范围，不再出现「回车选中了看不见的条目」。
- 自动加载更早的历史时，旧轮次一出现就是折叠好的最终态，不再先画成「运行中 / 展开」再收拢。
- 插件注入、项目指令、技能目录、运行时上下文等条目与工具行一起折进轮级过程按钮，标题统一为官方的「上下文注入 / 跨会话召回 / 系统提示词」。
- 底部工具栏按实测宽度取舍：权限按钮、模型切换、发送始终显示，其次是思考强度、附件、tps、上下文占用环，最宽裕时才显示权限名与 `44K/128K` 这类精确数值。
- 修复连续工具调用时思考 / 正文与工具行的顺序错位，现在与 DSH Web 一致。
- 纯新增文件在默认 `git.untrackedChanges: "mixed"` 下也会标 `[新增]`。

## 0.6.0（2026-09-13）

- 第一次点击文件芯片就能看到 diff（此前要连点两次）；轮尾的文件改动行改为本轮结束后才出现。
- 拖放文件到输入框能加附件，单文件上限 8 MB，超限 / 目录 / 读不出的项逐个明确提示；从资源管理器拖要先按住 `Shift`（已知限制）。
- 上传失败的芯片在悬停里给出真实原因，并可点击重试。
- 重载窗口或开第二个窗口后，文件芯片上的 `[新增]` 与删除线不再消失。
- 文件不再被误判为「已删除」：只有明确报「找不到」才算，权限不足、网络盘、路径异常不画删除线。
- 相对路径拿不到会话工作目录、按住修饰键点已删除芯片，两处此前点了完全没反应，现在都有明确提示。
- 待处理的审批卡 / 提问卡移到输入框上方常显，提问优先于审批；答过的卡仍留在对话流里当记录。
- 上下文条目按类型分述正文（指令增删改、技能目录条目、快照、跨会话召回等）。
- markdown 改为官方口径的软换行：单个换行按空格处理，只有行尾两个空格或反斜杠才断行。
- 认不出的内容块不再静默丢弃，显示一条「未知内容块」记录，可展开看内容。
- 工具行展开体分输入 / 输出两段并显示参数，折叠行右侧显示 `+N -M` 改动量，行内文件路径可直接点击预览。
- 代码块改为自动换行，窄侧栏里读长行不再需要横向拖到底。
- 一轮结束后，答案之前的过程折成一枚「N 次工具调用 · M 条消息 · K 个 subagent」按钮，点开可原样铺回；生成期间不折。
- 思考段默认折叠（流式中摘要取最后一行、结束后取第一行）；轮尾新增「用时 X」胶囊，点开显示本轮总用时、输出速度与首 token 用时。
- markdown 任务列表的复选框恢复显示；用户消息也补上操作行（复制、时间）。
- 目标条正文默认一行截断，暂停按钮左侧新增展开按钮可看全文，并支持条内内联编辑（`Enter` 保存、`Esc` 取消）。
- 助手消息里的图片块现在会显示出来，不再被静默丢弃。
- 已知限制：公式按纯文本显示、代码块不着色。

## 0.5.1

- 目标条现在可以正常清除，切换会话时不再残留；上一会话的上下文占用、速度、统计、token 用量、轮次大纲一并正确清空。
- 会话历史里能看到分支会话，按血缘深度缩进显示在源会话下面，标题带「分支: 」/「Fork: 」前缀。
- 斜杠命令列表不再截断命令名（宽度不够时先省略右侧描述）。
- 读取节点的行号紧贴文件名显示，不再飘到行尾。
- 同一个文件不再同时出现在「本轮文件改动」和「交付文件」两处。

## 0.5.0

### 破坏性变更

- 附件不再内联文件正文：图片走内容块，其余文件选中即上传，`@` 引用只在正文里放 `@path`（目录为 `@dir/`）。此前内联会让一个源文件吃掉几千 token。

### 新增

- 分支：任意助手回复可「从这里分支」，以该轮为界开一个新会话，原会话不动（生成中禁用）。
- 目标条：显示当前目标的阶段与进度，可暂停 / 恢复 / 清除。
- 上下文占用环：显示百分比，60% 起转黄、90% 起转红，悬停看构成明细。
- 「加载更早的消息」：跟随窗口只带 60 条，更早的内容可往前翻页。
- 斜杠命令节点：命令折成一行可展开的记录，界面上按钮发出的命令也有可见结果。
- 模型重试提示（「正在重试 n/m…」，恢复后收掉）；回答因 max-tokens 被截断时明确告知。
- `/` 菜单列出技能并标注「技能」。
- 设置项 `dshChat.language`（中 / 英 / 跟随 VS Code）与 `dshChat.fontSize`（小 / 中 / 大 / 跟随 VS Code），改完即时生效。

### 修复

- `/plan` 进出都走命令通道，退出改用 `/plan off`（此前进入不生效、退出方向反了）；手打的斜杠命令现在会执行，不再当普通消息发给模型。
- 目标面板与计划模式按钮恢复正确显示与响应。
- 交付文件可见；工具行状态点不再盖住图标，只有失败 / 已停止才画点，并补上已停止态。
- 终端退出码与信号中断从正文剥出并呈现，非零退出标记为失败。
- 工具结果里的图片、纯图片用户消息不再被丢弃；中止后未结算的工具行不再卡在「运行中」。
- 读取节点的行号紧贴完整文件名；停止按钮悬停有反馈；`@` 列表里选中目录默认打开该目录。
- 上下文占用率恢复正常刷新，占用条常驻并保留旧值，压缩后会下降。
- 崩溃后重启服务器不再被残留锁卡住（启动前清理无主锁）。

### 已知限制

- 停止语义与官方契约有一处刻意偏离：仍是「摘空队列 → 停止 → 等空闲 → 按原序重发」，因为服务端队列自行接续不稳定。
- `schedule`、`agentPreset`、`subagentTiming` 三个面板尚未提供。

## 0.4.1

- 修复思考与执行节点的状态指示；开启「减少动效」时不再漏掉其中一个。
- 残留进程清理修掉「拿不到命令行反而误杀」的缺陷。
- 「不认识的事件」告警只对真正未知的事件触发，已知但有意不渲染的事件不再刷噪音。

## 0.4.0

- 编辑类节点渲染结构化 diff（单栏 / 双栏，`dshChat.diffLayout`）。
- 自动载入的提示词（系统提示词 / 插件注入 / 项目指令 / 技能目录）作为节点可见。
- 队列「取回重新编辑」与 `ESC` 中止并发出队首。
- 通用文件入口：按内容分派内嵌或插入路径。

更早的版本见 git 历史与 `Releases/` 下的 vsix。
