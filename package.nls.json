{
  "container.title": "DSH Chat",
  "description": "Continue-style chat UI for DeepSeek Harness (dsh) inside VS Code. Runs `dsh web` in the background and renders the conversation natively in VS Code (bilingual UI).",
  "view.chat.name": "Chat",
  "cmd.newSession.title": "DSH: New Chat",
  "cmd.history.title": "DSH: Chat History",
  "cmd.openInEditor.title": "DSH: Open Chat in Editor",
  "cmd.stop.title": "DSH: Stop Generating",
  "cmd.addSelection.title": "DSH: Add Selection to Chat",
  "cmd.addFile.title": "DSH: Add File to Chat",
  "cmd.addFolder.title": "DSH: Add Folder to Chat",
  "cmd.startServer.title": "DSH: Start Server",
  "cmd.restartServer.title": "DSH: Restart Server",
  "cmd.stopServer.title": "DSH: Stop Server",
  "cmd.setToken.title": "DSH: Enter Access Token",
  "cmd.showLogs.title": "DSH: Show Logs",
  "cmd.showDiagnostics.title": "DSH: Show Diagnostics",
  "config.command": "Used to start the internal DSH when no external DSH service URL is set. **Takes effect after reloading the window.**",
  "config.url": "Base URL of an already running `dsh web` server (e.g. `http://127.0.0.1:3080`). **Once set it takes over completely**: `dshChat.command` and `dshChat.autoStart` stop applying and the extension connects straight to this address.\n\n**Reload the window for this change to take effect.**",
  "config.autoStart": "Let the extension run the start command by itself: when VS Code starts, and whenever no background server is running. Turn it off to keep full control — on startup the extension then only **checks whether a background is already running**: if it is, it connects and keeps reconnecting automatically; if not, it shows a **Start server** button instead of launching anything. Sending a message, switching sessions and the Start/Restart Server commands always launch a server when needed, regardless of this setting. Has no effect while `dshChat.url` is set.",
  "config.openPanelOnStartup": "Open the chat panel in the editor area when VS Code starts.",
  "config.startTimeoutSec": "How long to wait for the server to become ready, in seconds.",
  "config.supervisorIdleSec": "How many seconds the server keeps running after the last window closes, before it shuts down on its own. Too small a value stops it during a window reload.",
  "config.diffLayout": "How edit tool calls render their change: single column or side-by-side.",
  "config.diffLayout.auto": "Adaptive: single column in a narrow panel, side-by-side when wide.",
  "config.diffLayout.unified": "Always single column.",
  "config.diffLayout.split": "Always side-by-side.",
  "config.language": "Language of the chat UI. `auto` follows the VS Code display language.",
  "config.language.auto": "Follow the VS Code display language.",
  "config.language.zh": "Always Simplified Chinese.",
  "config.language.en": "Always English.",
  "config.fontSize": "Font size of the chat UI in integer pixels (>= 8). `0` follows the VS Code font size.",
  "config.questionBatch": "How many questions of one questionnaire to show at once. A questionnaire with more questions than this is asked one question at a time. `0` always shows every question at once."
}
