# DSH Chat 的领域语言

本仓库是 `dsh web` 的**自绘前端**（VS Code 扩展）：宿主（Node 侧）跟服务端对话、把状态折叠成
视图模型，界面（webview 侧）只负责画。这份文件是这套东西的**词表**——一个概念只用一个词，
别的写法列在 `_Avoid_` 里。实现细节不写在这里（那是代码注释与 `docs/design-*.md` 的事）。

## 会话与流

**会话**：
一段对话日志。扩展按会话 id 隔离状态，服务端是它的唯一真相来源。
_Avoid_: 聊天、对话、conversation、线程

**会话域**：
一个窗口打开某个会话时，扩展为它维护的一整套状态（跟随流、投影值、队列、模型选择、草稿……）。
域内的帧只发给绑定该会话的窗口；全局帧（连接、会话列表、模型目录）发给所有窗口。
_Avoid_: 会话状态、session state、上下文对象

**跟随流**：
`session/follow` 的长连接。先给一个**跟随开帧**（日志截点 + 记录 + 投影值），之后逐条推增量事件。
_Avoid_: 订阅、stream、channel

**跟随开帧**：
跟随流的第一个帧，是「截至某个 seq 的完整快照」。
_Avoid_: 快照、snapshot（在契约层面它是 `snapshot`，但讨论顺序问题时说「开帧」）

**投影**：
服务端把会话日志折叠成的**一整份当前值**（`plan` / `todos` / `goal` / `title`…）。
扩展只读它、绝不做自己的领域折叠。
_Avoid_: 派生状态、derived state、reducer、摘要

**投影键**：
投影的名字。契约里**客户端可见**的键是一份封闭清单。
_Avoid_: 投影名、projection id

**投影值存储**：
一个会话域里「每个投影键此刻是什么值」的权威表——**哪个值算数的唯一裁判**。
_Avoid_: 投影缓存、projection cache、状态表

**水位**：
服务端给每个投影值打的时间标记（增量帧的 `seq`、baseline 的 `asOfSeq`）。
一个值只在**比已存的水位更新**时才被接受；同水位也算旧。
_Avoid_: 序号、版本号、时间戳、version

**baseline**：
`session/control` 流给出的全量投影集合（按会话分键），带一个截止水位。
它是**替换型**的：块里**没带**的键表示能力缺失，旧值要清掉。
_Avoid_: 全量帧、初始化包

**能力缺失**：
一个投影键读出来是「没有」的状态（对应的服务端单元没加载，或还没有任何帧带到它）。
界面据此**不显示**，而不是显示一个零值或空壳。
_Avoid_: 空值、undefined、缺省

**控制流**：
`session/control` 的长连接：baseline + 队列 / 后台任务 / 投影的增量帧。
_Avoid_: 控制通道、control channel

## 一轮对话的形状

**段**：
助手消息里的一段可渲染内容（正文 / 思考 / 工具 / 命令 / 注入 / 交互卡 / 图片 / 未知块）。
_Avoid_: 节点、block、块、item

**轮次**：
一次用户提交引发的整轮生成，由 `turn/start` 与 `turn/end` 括起来。
_Avoid_: 回合、turn 之外的说法

**折叠**：
把一轮里的过程段收成一枚按钮；一轮只留最后那段正文。
_Avoid_: 收起、compact、合并

**轨迹账本**：
宿主按官方口径对**同一份 durable 事件**做的另一套折叠（含系统提示词、上下文、压缩这些
聊天流里根本不出行的东西）。它不是从消息流派生的。
_Avoid_: 轨迹面板数据、timeline model

**提交模式**：
运行中提交一条消息的方式：排队发送（queue）＝等下一轮，插话发送（steer）＝立刻进当前轮。
_Avoid_: 发送模式、send mode

## 后台与连接

**守护进程**：
唯一持有 `dsh web` 生命周期的独立进程。扩展只**请求**它，从不自己起服务器。
_Avoid_: supervisor 进程、后台服务、server manager

**会合文件**：
守护进程与扩展之间交换「服务器地址 + 令牌」的磁盘文件（今天只剩会合信息）。
_Avoid_: 锁文件、状态文件、token 文件

**粘性目标**：
这一轮连内部后台还是外部地址。自动路径选定后**不再换**；只有用户点按钮才换。
_Avoid_: 连接模式、target mode

**连接条**：
界面上那条显示连接状态与按钮的横条。它的三类状态（按钮态 / 失败要用户动作 / 正在连）
语义不同，不许合并渲染。
_Avoid_: 状态栏、banner、提示条
